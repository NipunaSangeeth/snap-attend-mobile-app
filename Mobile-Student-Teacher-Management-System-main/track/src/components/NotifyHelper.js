import React from 'react';
import { View, Text } from 'react-native';

const NotifyHelper = () => {
    // Notifications are disabled in Expo Go SDK 53+
    // To use remote notifications, a development build is required.
    return null;
};

export default NotifyHelper;
